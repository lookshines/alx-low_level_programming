#include <stdio.h>
/**
 * main - program the number of arguments
 *
 * @argc: number of arguments to program
 * @argv: argument to program
 *
 * Return: 0 for successful
 */
int main(int argc, char __attribute__ ((unused)) *argv[])
{
	printf("%d\n", argc - 1);
	return (0);
}
